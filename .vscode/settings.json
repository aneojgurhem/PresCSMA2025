{
    "latex-workshop.latex.external.build.command": "sh",
    "latex-workshop.latex.external.build.args": [
        "-c",
        "docker run --rm -v $PWD:/src -w /src -u $UID:$GID latex just compile",
    ],
}